// The public site's brand colours, mirrored from public/assets/styles/_variables.scss so the
// generated image prompts ask for pictures that look like they came from faunapoolen.se.
// Keep in sync with that file — it stays the source of truth for the site itself.
export const BRAND_PALETTE = Object.freeze({
  primary: '#00A1E4',
  secondary: '#00D3C8',
  accent: '#FF9F71',
  contrast: '#001E29',
  white: '#FFFFFF',
});
